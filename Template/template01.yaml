metadata:
  name: task-template-1
  namespace: argo
spec:
  templates:
    - name: task-template
      script:
        image: python:3.8-slim
        command: [python]
        source: |
          print("Task executed.")
